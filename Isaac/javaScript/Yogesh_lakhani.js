let you =" are the reason";
//using let, we assigned a string data type, allowing us to declare a variable that may be changed.

var we = " the next generation of programmer";
//using var, We declared we variable and assigned a string data type, allowing us to declare a variable that may be changed.

const us = ' we are upcoming software problem solvers';
//used const to assign us to the string data type, allowing us to declare a variable but preventing it from being changed.

let num1 = 5;
//declare variable num1 to an integer data type with the value 5, and the let specifies that it can be updated; however, 5 is an int value, thus there are no quotes around it.

let num2 = 10;
//declared variable num2 to an integer data type with the value 10, and the let specifies that it can be updated; however, 10 is an int value, therefore there are no quotes around it.

let num3 = num2 += num1;
//Declared the variable num3, initialized it with the sum of num2 and num3, and then: output = 15

let ans = num1 % num2;
//declared a variable called ans and initialized it to determine the remainder/modulus of numbers num1 and num2.

console.log(ans);
// this will print the output of line 19 in this case the modulus of 5 % 15 is 5.

console.log(num3 *= 2);
// will print the output of line 16 which is 15 and multiply by 2; output : 30

var num4;
//declared an uninitialized variable with the name num4.

console.log(num4);
//print the value of the num4 variable. Given that variable num4 is not initialized, this yields "undefined".


console.log(2num6);
//SyntaxError: Unexpected or invalid token. Names for variables cannot start with a number.

var ans = 'this is not right';
//this will return an error because variable ans has already been declared

var let ='do you know what you are doing?';
//The javascript keyword let is used to identify a variable. Although the syntax is correct, the identifier does not follow JavaScript's guidelines for variable naming.

const dad;
//This will result in an error since the const needs to be initialized before it can be declared.

const myNum = num1>num2? num3:us;
//The keyword const is used to declare the variable myNum but its initial value depends on the comparison made between if num1 > num2 if true, num3 is assigned to myNum otherwise us.

console.log(--num3);
//This means to decrease the value of num3 by -1 before printing it. The result is 29

console.log(num3);
//This will print the value of num3 which is 29

console.log(num3++);
//This will print the value, which is 29, first and then increase it by one.

console.log(num2 != num3);
//Depending on if the value in the variable num2 is not equal to that in num3, this will return a boolean. This is True in this instance.

console.log(num3 !=num2 && num2==num2);
//print true or false depending on if the value in num3 differs from the value in num2 and whether the value in num2 equals to the value in num2. This returns true in this instance.


if(100>=200){
	const num2 =20;
	console.log('you are now a junior developer');
}
// As a result of the condition being false, this block will not be executed.














